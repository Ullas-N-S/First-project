file 				- #to check file type
du 				- #disk usage
-h 				- #Human readable
. 				- #Current Directory
.. 				- #Parent Directory
/ 				- #Root Directory
~ 				- #Home Directory
ls *.txt 			- #Finds every file ending in .txt
rm test* 			- #Targets every file starting with "test", such as test1.log or testing_script.sh
cp photo.jpg backup.jpg 	- #Creates a second copy named backup.jpg in the same folder.
cp photo.jpg ./images/  	- #Moves a duplicate of the file into the images directory.
rm 				- #remove
rm -i                           - #remove with interactive flag that asks before performing for each file
rm -v				- #remove with verbose flag to display a message in the terminal for every file that is successfully deleted
cp *data* /backup 		- #Matches any file with "data" anywhere in its name
cp *data /backup 		- #Matches any file ending with "data" in its name
find [path] [expression] 	- #searches for files and directories in a directory 
echo "HelloWorld" 		- #echos HelloWorld
find . -size +100M 		- #Find files larger than 100MB
find /home -type d 		- #Find only directories; use -type f for files
\ 				- #For paces or to continue in next line
-type 				- #Filters by file type
-size 				- #Filters by size
pwd 				- #Shows Current Working Directory
cd -  				- #Takes you to previous working direcory 
ls -l 				- #link flag that lists with user,group,modified time
ls -r 				- #lists in revers alphabetical order
ls -a 				- #lists all files and folders in the folder, even hidden
ls -lu 				- #lists with -l flag and with the time of user last accessed
ls -lc 				- #lists with -l flag and with the time of user last changed
echo "New Line" >> filelist.txt - #String is appended to the file while, if not exists, then created and added
touch mysuper 			- #Creates an empty file named mysuper. If it already exists, it updates the access and modification times to "now"
touch -r file1.txt file2.txt 	- #Reference mode. It sets the timestamps of file2.txt to match those of file1.txt
touch -d "2023-01-01 12:30:00" mysuperduperfile  - #Date mode. It sets the file's timestamp to the specific date and time provided instead of the current time
stat filename 			- #Displays detailed status of a file
which 				- #Locates the executable path of a command
mv [old_name] [new_name]	- #To rename a file
cat > new.txt                   - #Creates a new text file
cat new1.txt new2.txt           - #Displays both files content one after the other 
cat -n new.txt                  - #displas content with every line numberd, even empty lines
cat -b new.txt                  - #displays content with lines numbered and not empty lines
cp -f mycoolfile /home/pete/Pictures	- #-f flag to force copy
cp -p mycoolfile /home/pete/backups/	- #Preserving File Attributes with cp -p
less new.txt                    - #Used to open file with large content
	/search_term	to search word in the file
		n	to jump to next occurance of that word
		N	to jump to previous occurance of that word
	q	to quit the less viewer
history                         - #To display History of commands
	Ctrl+R		to reverse search
	history -c 	to clear complete history
	history -d 101	to delete 101th command from history

